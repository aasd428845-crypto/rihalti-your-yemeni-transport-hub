import SupplierPayments from "@/pages/supplier/SupplierPayments";

// Reuse the same component — delivery companies see their direct payments the same way
const DeliveryPayments = SupplierPayments;
export default DeliveryPayments;
